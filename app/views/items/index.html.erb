<h1>Items#index</h1>
<p>Find me in app/views/items/index.html.erb</p>

<ul>

  <% @items.each do |item| %>
    <li> <%= User.find(item.user_id)[first_name]%>'s <%= item.category_type%>,
    <br> <small> <%= item.description%> </small>
    </li>
  <% end %>

</ul>
