<div class="cards">
      <% if @items.any? %>
        <% @items.each do |item| %>
          <div>
              <div class="clothing-card-main <%= 'your-wardrobe' if User.find(item.user_id) == current_user %>">
                <div class="group-3">
                  <div class="">
                    <h4 class="name">
                      <%= item.colour%>
                      <%= item.category%>
                    </h4>
                  </div>
                  <div class="button-class">
                    <button class="button"><%= link_to "View", item_path(item), {:style=>'color:#FFFFFF;'} %></button>
                  </div>
                  </div>
                <div>
                  <%= cl_image_tag item.photos.first.key, height: 356, width: 270, crop: :pad, background: "auto"  %>
                </div>
                <div class="group-2">
                  <h3 class="subheader">
                    <div>
                      <% if User.find(item.user_id) == current_user %>
                        <strong> Your wardrobe </strong>
                      <% else%>
                        <%= User.find(item.user_id)[:first_name]%>'s wardrobe
                      <% end %>
                    </div>
                  </h3>
                  <p class="size"><%= "Size #{item.size}" %></p>
                </div>
              </div>
          </div>
        <% end %>
      <% else %>
        <em>Sorry, no items found, would you like to make a request?</em>
      <% end %>
</div>


<style>
/* CSS */
.button-56 {
  align-items: center;
  border: 2px solid #111;
  border-radius: 8px;
  box-sizing: border-box;
  color: #111;
  cursor: pointer;
  display: flex;
  font-family: Inter,sans-serif;
  font-size: 16px;
  height: 48px;
  justify-content: center;
  line-height: 24px;
  max-width: 100%;
  padding: 0 25px;
  position: relative;
  text-align: center;
  text-decoration: none;
  user-select: none;
  -webkit-user-select: none;
  touch-action: manipulation;
}

.button-56:after {
    display: none;
  border-radius: 8px;
  content: "";
  display: block;
  height: 48px;
  left: 0;
  width: 100%;
  position: absolute;
  top: -2px;
  transform: translate(8px, 8px);
  transition: transform .2s ease-out;
  z-index: -1;
}

.button-56:hover:after {
  transform: translate(0, 0);
}

.button-56:active {
  background-color: #F38181;
  outline: 0;
}

.button-56:hover {
  outline: 0;
}

@media (min-width: 768px) {
  .button-56 {
    padding: 0 40px;
  }
}

/* clothing_card so I don't have 2 different card layouts */
/* class="row justify-content-center"  */

.clothing-card-main {
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    gap: 10px;
    min-width: 303px;
    max-height: height: 420px;
    background: #F8F7F7;
    margin: 5px;
    border:none;
}

.clothing-card-main.your-wardrobe {
  background-color: #F9A2A2;
}

.group-3 {
    width: 264px;
    height: 33px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.name {
    font-family: 'Inter', sans-serif;
    font-style: normal;
    font-weight: 500;
    font-size: 20px;
    line-height: 29px;
    color: #000000;
}


.button {
  color: white;
  border: none;
  outline: none;
  width: 100px;
    opacity: 0;
    border-radius: 10px;
    transition: opacity 0.2s ease-in-out;
    background-color: #F38181;
}

.clothing-card-main:hover .button {
  opacity: 1;
}

.button:active {
  outline: 0;
}

.button:hover {
  outline: 0;
}


.rectangle-10 {
    width: 263px;
    height: 350px;
    background: #D6D5D5;
    border-radius: 16px;
}

.group-2 {
    width: 263px;
    height: 74px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.size {
    font-family: 'Inter', sans-serif;
    font-style: normal;
    font-weight: 400;
    font-size: 20px;
    line-height: 24px;
    text-align: right;
    color: #000000;
}

.subheader {
    font-family: 'Inter', sans-serif;
    font-style: normal;
    font-weight: 400;
    font-size: 20px;
    line-height: 24px;
    color: #000000;
}

.description {
    width: 263px;
    max-height: 57px;
    font-family: 'Inter', sans-serif;
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    line-height: 19px;
    color: #000000;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: normal;
}

.cards {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  grid-gap: 16px;
}

@media(max-width: 992px) {
  .cards {
    grid-template-columns: 1fr 1fr;
  }
}

@media(max-width: 768px) {
  .cards {
    grid-template-columns: 1fr;
  }
}

img {
  border-radius: 5px;
  width: 100%;
}

</style>