<h1>Requests#show</h1>
<p>Find me in app/views/requests/show.html.erb</p>


<%= @request.user_id%> <br>
<%= @request.title%> <br>
<%= @request.description%> <br>
<%= @request.status%> <br>


<% if @request.status == "Closed" %>
  <% choice = Item.find(@request.item_id) %>
  <%= User.find(choice.user_id) %> - <%= choice.category %>
<% end %>


<h2>Suggestions</h2>
<% console %>
<ol>

  <% @request.items.each do |item| %>
    <li>
    <%= User.find(item.user_id)[:first_name]%>
    <%= item.category%>,
    <%= link_to "View", item_path(item), class: "btn" %>


    </li>
  <% end %>

</ol>

  <%= link_to "Edit", edit_request_path(@request), class: "btn" %>



<%# TO DO

Add card
Add link %>
