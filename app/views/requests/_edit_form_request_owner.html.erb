<%= simple_form_for(request) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.collection_select :item_id, Item.all, :id, :description, prompt: true %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit submit_text %>
<% end %>
